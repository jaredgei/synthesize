# Builds and bundles a production build of a react.js project.
# This should only be run through the node script "bundle"
# associated with this project.
npm run build-css && # build css
npm run build # build project
